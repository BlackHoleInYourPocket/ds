package linkedlist

import linkedlist.queueu.ArrayListQueue
import linkedlist.queueu.LinkedListQueue
import linkedlist.trees.TreeNode
import linkedlist.trees.makeBeverageTree
import stack.Stack

fun main(args: Array<String>) {
    //Test here
    val singleLinkedList = SingleLinkedList<Int>().apply { }
    val doubleLinkedList = DoubleLinkedList<Int>().apply { }
    val stack = Stack<Int>().apply { }
    val arrayListQueue = ArrayListQueue<Int>().apply { }
}
